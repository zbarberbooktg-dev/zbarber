name: Deploy — vitrine (test) → test.zbarber.net

# Manual only: this never runs automatically.
on:
  workflow_dispatch:

concurrency:
  group: deploy-test
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || '22' }}
          script: |
            set -euo pipefail
            REPO=/srv/zbarber/test
            git -C "$REPO" fetch --depth=1 origin test
            git -C "$REPO" reset --hard origin/test
            bash "$REPO/deploy/deploy.sh" vitrine test
